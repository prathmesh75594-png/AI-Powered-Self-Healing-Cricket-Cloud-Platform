apiVersion: v1
kind: Service
metadata:
  name: cricket-frontend-service
spec:
  type: NodePort
  selector:
    app: cricket-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080